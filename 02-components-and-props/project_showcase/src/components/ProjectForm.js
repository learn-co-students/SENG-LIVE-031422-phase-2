function ProjectForm({ projects }) { // { projects: []}
  // destructure our props object 

  // const {projects} = props // so we don't have to keep doing props.project
  return <div>ProjectForm</div>;
}

export default ProjectForm;
